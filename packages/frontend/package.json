{
  "name": "frontend",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "test:unit": "vitest",
    "subset": ".\\scripts\\env\\Scripts\\activate && python ./scripts/subset.py",
    "format": "prettier --write src/"
  },
  "dependencies": {
    "common": "workspace:^",
    "ui": "workspace:^"
  }
}
